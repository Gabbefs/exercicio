// Introdução a Arrays
//For Each

// ver exercícios gu

// combinando arrays

const numeros = [1,2,3,4,5];

const combinado = numeros.join('.');
console.log(combinado);

const frase = 'ola bem vindo ao curso';
const resultado = frase.split(''); // divide com base no espaço, retornando um array de palavras
console.log(resultado);

console.log(resultado.join('-')); // adiciona um traço para separar os elementos
